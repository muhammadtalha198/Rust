// loops in Rust

fn main() {
    //while loop in rust

    let mut a = 1;
    while a <= 10 {
        println!("The value of a is {}", a);
        a += 1;
    }

    // for loop in rust

    for x in 1..=10 {
        println!("The value of x is {}", x);
    }
}
